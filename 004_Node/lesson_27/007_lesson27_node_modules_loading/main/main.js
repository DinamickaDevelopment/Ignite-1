// Если идентификатор модуля, переданный функции require() не является встроенным модулем и не начинается с 
// '/', '../' или './', то NodeJS начинает поиск в родительской директории, создает папку node_modules, 
// если она еще не создана и пробует загрузить модуль из нее 

var node_static = require('sample_module');

if (node_static != undefined) {
    console.log('module resolved!'); 
}